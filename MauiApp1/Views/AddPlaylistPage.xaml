<?xml version="1.0" encoding="utf-8" ?>
<toolkit:Popup xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
               xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
               xmlns:local="clr-namespace:MauiApp1.Views"
               xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
               xmlns:vm="clr-namespace:MauiApp1.ViewModels"
               x:Class="MauiApp1.Views.AddPlaylistPage"
               Color="{StaticResource bg00dp}">
    <toolkit:Popup.BindingContext>
        <vm:AddPlaylistViewModel/>
    </toolkit:Popup.BindingContext>
    <VerticalStackLayout Padding="20">
        <Label 
            Text="Add playlist"
            FontAttributes="Bold"
            FontSize="Title"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />
        <Grid>
            <Label Text="From YouTube?"
                   VerticalOptions="CenterAndExpand"
                   HorizontalOptions="StartAndExpand"/>
            <Switch VerticalOptions="CenterAndExpand"
                    HorizontalOptions="EndAndExpand"/>
        </Grid>
        <ContentView>
            <ContentView.Content>
                <local:PlaylistYoutubeView/>
            </ContentView.Content>
        </ContentView>
    </VerticalStackLayout>
</toolkit:Popup>
